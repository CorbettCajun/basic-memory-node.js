# Basic Memory Environment Configuration

# Home directory for storing Markdown files
BASIC_MEMORY_HOME=~/basic-memory

# Database path (optional)
# Specify a custom path for the SQLite database file
# If not specified, defaults to BASIC_MEMORY_HOME/basic-memory.db
BASIC_MEMORY_DB_PATH=

# MCP Server configuration
BASIC_MEMORY_PORT=8765
BASIC_MEMORY_HOST=localhost

# Logging configuration
BASIC_MEMORY_LOG_LEVEL=info
# Options: error, warn, info, debug, trace

# Database configuration
# SQLite database will be stored in the home directory by default
# No need to change this unless you want to use a specific location

# File synchronization
SYNC_TO_FILES=true
# Set to "true" to enable syncing entities to files, "false" to disable

# SQL Logging (for debugging)
SQL_LOGGING=false
# Set to "true" to log SQL statements, "false" to disable
